# Glove Detection

This project contains machine learning models and notebooks for glove detection.

## Directory Structure
- `models/`: Trained model files.
- `notebooks/`: Jupyter notebooks (e.g., `fine_tune.ipynb` for fine-tuning models).
- `submission/`: Submission related files.
